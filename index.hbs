<!DOCTYPE html>

<html>
  <head>
    <link rel="stylesheet" href="http://fonts.googleapis.com/css?family=Oswald:400,700,300|Pacifico" type="text/css">
    {{{styles}}}
    <!-- <script src="./js/vendor/jquery-1.11.0.min.js"></script>
    <script src="./js/main.js"></script> -->
  </head>

  <body>
    <div class="{{#if card_class}} {{card_class}}{{else}}patient{{/if}}{{#if avatar}} avatar{{else}} no-avatar{{/if}} card">

      <!-- Greeting -->
      <div class="greeting">
        <div class="photo target small">
          <div class="outline"></div>
          <div class="image circle-crop"><img src="{{#if target.photo}}{{target.photo}}{{else}}{{base_url}}/img/default-avatar-{{#if card_class}}{{card_class}}{{else}}patient{{/if}}.jpg{{/if}}"></div>
        </div>
        <div class="salutation light-shadow">{{#if target.title_short}}{{target.title_short}} {{else}} Sen/Rep{{/if}}{{#if target.last}} {{target.last}}{{/if}},</div>
      </div>

      <!-- Main Copy -->
      <div class="main-copy">
        <div class="photo target large">
          <div class="outline"></div>
          <div class="image circle-crop"><img src="{{#if target.photo}}{{target.photo}}{{else}}{{base_url}}/img/default-avatar-{{#if card_class}}{{card_class}}{{else}}patient{{/if}}.jpg{{/if}}"></div>
        </div>
        <h1 class="centered shadow">
          {{#if card_text_start}}
            {{{card_text_start}}}<strong>{{city}}</strong>{{{card_text_end}}}
          {{else}}
            Don't make it even harder for Medicare patients and their families in <strong>{{city}}</strong>. <strong>Repeal the SGR now</strong> and avoid this disaster.
          {{/if}}
        </h1>
      </div>

      <!-- Signature -->
      <div class="signature light-shadow">
        <div class="photo signer">
          <div class="outline"></div>
          <div class="image circle-crop">{{#if avatar}}<img src="{{avatar}}">{{/if}}</div>
        </div>
        <div class="signer-info">
          <div class="name">{{first}}{{#if last}} {{last}}{{/if}}</div>
          <div class="location">{{#if city}}{{city}}{{/if}}{{#if state}}, {{state}}{{/if}}</div>
        </div>
      </div>

      <!-- Footer -->
      <div class="footer">
        <div class="footer-content centered">
          <span class="ama-logo"></span>
          <span class="bullet">&bullet;</span>
          <span class="link">Send a message to your representatives at <strong>action.fixmedicarenow.org</strong></span>
        </div>
      </div>

      <!-- Background -->
      <div class="card-bg">
        <div class="dark-layer"></div>
        <div class="color-layer">
          <div class="color top"></div>
          <div class="color bottom"></div>
        </div>
      </div>
    </div>

    {{{scripts}}}
  </body>
</html>